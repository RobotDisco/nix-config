diff --git i/src/local.rs w/src/local.rs
index 9c5f5d0..5dd2666 100644
--- i/src/local.rs
+++ w/src/local.rs
@@ -121,7 +121,16 @@ impl Local {
             })?;
 
         // Build the query to search for all mail in our maildir.
-        let all_mail_query = format!("path:\"{}/**\"", relative_mail_dir.to_str().unwrap());
+        let all_mail_query = if relative_mail_dir.as_os_str().is_empty() {
+            // When the maildir IS the notmuch database root,
+            // the format string would produce "path:\"/**\""
+            // which notmuch >=0.32 mis-parses as a regex
+            // (leading `/` opens regex mode). Use the
+            // semantically equivalent catch-all instead.
+            "path:**".to_string()
+        } else {
+            format!("path:\"{}/**\"", relative_mail_dir.to_str().unwrap())
+        };
 
         // Ensure the maildir contains the standard cur, new, and tmp dirs.
         let mail_cur_dir = canonical_mail_dir_path.join("cur");
