export XKB_DEFAULT_OPTIONS=ctrl:nocaps
export PS1='[\u@\h \W]$ '

if command -v keychain >/dev/null 2>&1; then
  keychain --eval --agents ssh id_rsa id_rsa.work
fi

if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then
  exec sway
fi
