export XKB_DEFAULT_OPTIONS=ctrl:nocaps
export PS1='[\u@\h \W]$ '

command -v keychain >/dev/null 2>&1 && eval "$(keychain --quiet --agents ssh --eval --noask)"

if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then
  exec sway
fi
