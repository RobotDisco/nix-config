#+TITLE: TODOs
#+AUTHOR: Gaelan D'costa

Things I should do eventually

* Emacs Learning Curriculum

** Phase 1: Core Emacs Fluency
- [ ] Window management: =C-x 2/3=, =winner-mode=, =C-x o=
- [ ] Mark ring: =C-SPC= to set, =C-u C-SPC= to travel back
- [ ] Keyboard macros: =C-x (=, =C-x )=, =C-x e=
- [ ] Registers: store/recall positions, text, rectangles
- [ ] Rectangles: =C-x r k/y= for column editing

** Phase 2: Completion Stack
- [X] vertico
- [X] orderless
- [X] marginalia
- [X] consult
- [X] embark

** Phase 3: Project & File Navigation
- [X] project.el (built-in, replaces projectile): =C-x p f= find file,
  =C-x p b= project buffer (via consult), =M-s r= ripgrep project
- [ ] dired: mark files with =m=, operate with =D/R/C=
- [X] consult-ripgrep: live project-wide search with preview (=M-s r=)

** Phase 4: Version Control
- [ ] magit deeply: interactive rebase, blame, log (2-3 focused sessions)
- [ ] forge: =forge-list-pullreqs=, PRs and issues without leaving Emacs
- [X] diff-hl: git diff indicators in fringe, updates on magit refresh

** Phase 5: IDE Features
- [ ] xref: =M-.= go-to-definition, =M-,= pop back, =M-?= find references
- [ ] flymake: =M-n=/ =M-p= to navigate diagnostics
- [ ] apheleia: format-on-save per mode (needs per-language formatter config)
- [ ] consult-eglot: workspace symbol search via consult (=xref-find-apropos= covers most of it already)
- [ ] dape: DAP debugger (more modern than dap-mode; wait until you want step-debugging)
- [ ] tree-sitter: better highlighting and structural navigation
- [ ] eglot per language: pyright (Python), solargraph (Ruby), Lisp server

** Phase 6: Zettelkasten / Org
- [ ] org-modern: cleaner org visuals (unicode bullets, aligned tables) — purely aesthetic
- [ ] org-capture: =C-c c= to capture without breaking flow
- [ ] org-agenda: =C-c a= entry point
- [ ] org-roam core loop: node-find, node-insert, backlinks buffer
- [ ] citar: =citar-insert-citation=
- [ ] org-babel: execute code blocks inline; run kubectl from notes

** Phase 7: DevOps / Kubernetes / SSH
- [ ] tramp: =C-x C-f /ssh:host:/path= — edit remote files as if local
- [ ] vterm: =vterm-copy-mode=, multiple vterm buffers per project
- [ ] kubernetes.el: kubectl UI — browse pods, logs, describe resources
- [ ] ob-shell: run kubectl from org runbooks, capture output inline
- [ ] tramp + dired: browse remote filesystems over SSH

** Phase 8: Power Features
- [X] avy: =C-'= jump to any visible character, =M-g j= goto line
- [X] embark: =C-.= embark-act, =C-;= embark-dwim, =embark-export= to grep buffer
- [X] wgrep: edit embark-exported grep buffers in-place across files
- [X] cape: dabbrev + file path completion sources for corfu
- [ ] iedit: edit all occurrences of a symbol simultaneously
- [X] which-key: built-in Emacs 30, enabled

** Phase 9: Language Depth
- [ ] Lisp: deepen paredit; geiser-guile configured (Racket dropped)
- [ ] Python: eglot + pyright + apheleia (black)
- [ ] Ruby: eglot + solargraph

- Explain the difference between my concept of profiles and modules (as internalized from https://www.reddit.com/r/NixOS/comments/um1ohu/how_do_you_write_custom_nixos_modules/)
- [X] Make my home-manager config work on both MacOS and Linux
- Figure out seamless way to run nix tasks from emacs and/or simple shell commands (devShell?)
- [X] Merge emacs nix flake back in here.
- Rewrite README.org to:
  - Recommend things to reduce the amount of time it takes to understand Nix.
  - Explain the important thing in my nix flakes since a lot of it basically just how I wrote it.
- Reconcile darktower and arrakis profiles so that I can use my nixos modules on both
- [X] Get gpg-agent working in MacOS
- [X] Re-evaluate nix-darwin for MacOS, see if it is worth it.
- [X] Add Makefile-like tasks: justfile with build-all, test-switch, apply,
  check, lint, fmt, check-emacs, emacs-dev, emacs-dev-package, etc.
- Rewrite apps section to encapsulate app definitions from the transformation code I use to minimize boilerplate
- Lean on github actions for now for:
  - dependabot (or something for flake updates)
  - cachix (so I don't have to recompile across instances?)
- Find graceful way to install linux/MacOS shared apps from single source
- Update docs to note that MacOS requires following installs before I can converge my nix-config
  - Nix
  - Homebrew
  - Nix-darwin
- Attempt to modify nix-darwin to work with hardware-specific keyboard mappings
- okta-awscli should be a publicly accessible package I feel
  - same with my fork of brother-laser
- After getting github/CI to automatically update flakes, pin nix/darwin autoupgrades to flake
  https://discourse.nixos.org/t/flakes-and-autoupgrade/12994
- make a service for uhk-agent
- Switch from Hyprland to SwayFX for better multi-monitor stability while
  keeping compositor effects (blur, transparency). Goal: Emacs as primary
  environment with semi-transparent frame over wallpaper (swww/swaybg +
  pgtk Emacs alpha-background + SwayFX window blur rules).
- Set up home-manager for darktower using gaelan-personal.nix base profile
  so that shell, ssh, dev tools, and yubikey config apply on the server.
- Revisit aerospace keybindings with intention: audit the full sway/i3
  default binding list against what aerospace can and can't do, and make
  deliberate choices for each gap (e.g. Return in resize mode, scratchpad
  approximation, focus mode toggle).
