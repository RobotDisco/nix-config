{
  # This setting is deprecated, so we're setting it to false.
  networking.useDHCP = false;

  # Servers are configured to be Canadian, Southern-Ontarian specifically.
  i18n.defaultLocale = "en_CA.UTF-8";
  time.timeZone = "America/Toronto";
}
