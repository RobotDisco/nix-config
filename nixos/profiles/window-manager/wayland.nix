{
  programs.sway.enable = true;

  # Historical notes
  # environment.systemPackages = [
  # Not tiling enough
  # pkgs.river
  # listens to XKB_DEFAULT_OPTIONS variable for X settings?
  #  pkgs.sway
  # Not a compositor but a starting point for writing your own
  # pkgs.mir
  # Too gimmicky
  # pkgs.hyprland
  # needs to be explicitly started in wayland mode; no hidpi by default
  #  pkgs.qtile
  # Default terminal
  # pkgs.foot
  # Kinda worse than foot
  # pkgs.kitty

  # Don't need Kanshi with sway
  # pkgs.kanshi
  # menu things
  # dmenu_path is what prints runnable commands
  # pkgs.dmenu-wayland
  # an emacs-bound bar prompt; we ignore the actual graphical command
  # pkgs.wmenu

  # Not needed systemwide, something I use in my local config
  # pkgs.brightnessctl
  # ];
}
